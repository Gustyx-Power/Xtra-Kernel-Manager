<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme -->
    <style name="Base.Theme.XtraKernelManager" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <style name="Theme.XtraKernelManager" parent="Base.Theme.XtraKernelManager" />

    <!-- Splash Activity Theme (untuk custom Compose splash) -->
    <style name="Theme.XtraKernelManager.Splash" parent="Base.Theme.XtraKernelManager">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">false</item>
    </style>
</resources>