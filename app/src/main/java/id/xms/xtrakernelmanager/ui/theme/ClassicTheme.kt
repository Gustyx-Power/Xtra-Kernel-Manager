package id.xms.xtrakernelmanager.ui.theme

import androidx.compose.ui.graphics.Color

object ClassicColors {
    // Nebula Core Palette - Google AI Stitch Design System
    val Background = Color(0xFF080f11) // Deep matte foundation
    val Surface = Color(0xFF101b1e) // Standard cards (surface_container)
    val SurfaceVariant = Color(0xFF19282c) // Active/Hover states (surface_container_highest)
    val SurfaceContainerLow = Color(0xFF0b1517) // Slightly recessed
    val SurfaceContainerLowest = Color(0xFF000000) // Deepest recessed areas
    val SurfaceContainer = Color(0xFF101b1e) // Standard container
    val SurfaceContainerHighest = Color(0xFF19282c) // Highest container
    val SurfaceContainerHigh = Color(0xFF142225) // High container
    val SurfaceBright = Color(0xFF1d2f33) // Bright surfaces
    
    val Primary = Color(0xFFadf4ff) // Luminous cyan glow
    val PrimaryDim = Color(0xFF5dddee) // Dimmed primary
    val PrimaryContainer = Color(0xFF6debfd) // Primary container
    val OnPrimary = Color(0xFF005e68) // Text on primary
    val OnPrimaryContainer = Color(0xFF00555e) // Text on primary container
    
    val Secondary = Color(0xFF66d9cc) // Teal accent
    val SecondaryDim = Color(0xFF56cbbe) // Dimmed secondary
    val SecondaryContainer = Color(0xFF002b27) // Secondary container
    val OnSecondary = Color(0xFF004943) // Text on secondary
    val OnSecondaryContainer = Color(0xFF3cb5a9) // Text on secondary container
    
    val Accent = Color(0xFFadf9f1) // Tertiary luminous
    val Error = Color(0xFFff716c) // Error red
    val ErrorDim = Color(0xFFc94947) // Dimmed error
    val ErrorContainer = Color(0xFF8a1a1e) // Error container
    val OnError = Color(0xFF490006) // Text on error
    
    val OnBackground = Color(0xFFd7e8ed) // Text on background
    val OnSurface = Color(0xFFd7e8ed) // Text on surface
    val OnSurfaceVariant = Color(0xFF9daeb3) // Muted text
    val Outline = Color(0xFF68787d) // Outline color
    val OutlineVariant = Color(0xFF3b4a4f) // Variant outline (20% opacity for ghost borders)
    
    // Status colors
    val Good = Color(0xFF84f5e8) // Positive status (secondary_fixed)
    val Moderate = Color(0xFFffc107) // Warning (keeping amber)
    val Critical = Color(0xFFff716c) // Critical status (error)
}

